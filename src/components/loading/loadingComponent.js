import React, { Component } from 'react';

class loadingComponent extends Component {
  render() {
    return (
      <div className="loadingComponent" class="loader">Loading...</div>
    );
  }
}

export default loadingComponent;
